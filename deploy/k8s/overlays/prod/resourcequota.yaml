apiVersion: v1
kind: ResourceQuota
metadata:
  name: joossam-quota
  namespace: joossam
spec:
  hard:
    requests.memory: "400Mi"
    requests.cpu: "150m"
    # limits.memory removed to prevent quota enforcement (optional for flexibility)
    pods: "9"
