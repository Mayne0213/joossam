apiVersion: v1
kind: ResourceQuota
metadata:
  name: joossam-quota
  namespace: joossam
spec:
  hard:
    requests.memory: "400Mi"
    requests.cpu: "150m"
    limits.memory: "800Mi"
    limits.cpu: "400m"
    pods: "9"
