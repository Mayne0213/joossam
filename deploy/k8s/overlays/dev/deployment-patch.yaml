apiVersion: apps/v1
kind: Deployment
metadata:
  name: joossam-app
  labels:
    environment: development
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: joossam-app
          resources:
            requests:
              memory: "40Mi"
              cpu: "15m"
            limits:
              memory: "150Mi"
              cpu: "50m"
